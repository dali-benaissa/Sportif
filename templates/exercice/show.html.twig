{% extends 'base.html.twig' %}

{% block title %}Exercice{% endblock %}

{% block body %}
    <h1>Exercice</h1>

    <table class="table" border="2">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercice.id }}</td>
            </tr>
            <tr>
                <th>Code_exercice</th>
                <td>{{ exercice.codeExercice }}</td>
            </tr>
            <tr>
                <th>Mouvement</th>
                <td>{{ exercice.Mouvement }}</td>
            </tr>
            <tr>
                <th>Desc_exercice</th>
                <td>{{ exercice.descExercice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exercice_index') }}">back to list</a>

    <a href="{{ path('exercice_edit', {'id': exercice.id}) }}">edit</a>

    {{ include('exercice/_delete_form.html.twig') }}
{% endblock %}
